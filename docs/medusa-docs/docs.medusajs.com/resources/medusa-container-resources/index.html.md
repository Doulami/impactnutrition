# Medusa and Module Container Resources

This reference provides the list of resources registered in the Medusa container and module containers.

## Medusa Container Resources

The following table lists all resources that you can resolve in your customizations outside a module (such as API routes, workflows, subscribers, and scheduled jobs).

Refer to the [Medusa Container](https://docs.medusajs.com/learn/fundamentals/medusa-container) documentation for more information on how to use these resources.

You can use the `ContainerRegistrationKeys` enum imported from `@medusajs/framework/utils` where specified.

|Resource|Description|Registration Name|
|---|---|---|
|Medusa Configurations|The configurations defined in |\`configModule\`|
|Logger|Utility to log messages to the terminal.|\`logger\`|
|Query|Query to retrieve data across modules.|\`query\`|
|Link|Manage links between records of two modules' data models.|\`link\`|
|Knex|Knex query builder|\`\_\_pg\_connection\_\_\`|
|Modules' main services|The main service of every module added in ||

***

## Module Container Resources

The following table lists all resources that you can resolve in a module's services or loaders.

Refer to the [Module Container](https://docs.medusajs.com/learn/fundamentals/modules/container) documentation for more information on how to use these resources.

Use the `ContainerRegistrationKeys` and `Modules` enums imported from `@medusajs/framework/utils` where specified.

|Resource|Description|Registration Name|
|---|---|---|
|Logger|Utility to log messages to the terminal.|\`logger\`|
|Entity Manager|An instance of |\`manager\`|
|Base Repository|An instance of the base repository, used to run transactions and perform database operations.|\`baseRepository\`|
|Medusa Configurations|The configurations defined in |\`configModule\`|
|Event Module|The service of the Event Module, used to emit events.|\`event\_bus\`|
|Services generated by the |The service of every data model passed to the |Each service is registered as the camel-cased name of the data model. For example:|
|Module services|You can resolve the module's services from one another, but not in loaders. Learn more in the |Each service is registered as the camel-cased name of the service. For example:|
